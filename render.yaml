services:
  - type: web
    name: kitescout-pwa
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn kitesurf.api.main:app --host 0.0.0.0 --port $PORT
    autoDeploy: true
